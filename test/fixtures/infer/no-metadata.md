# A page nobody ever gave frontmatter

This file has no metadata block at all. It passes a require-nothing schema and
fails the moment any key becomes required — which is the surprise the coverage
report exists to show before you ratchet.
