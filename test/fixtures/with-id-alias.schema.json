{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/docmeta/with-id.schema.json",
  "title": "The same schema reached by a second reference",
  "description": "Deliberately repeats with-id.schema.json's $id. Ajv keys its registry by $id, so this is what a schema named twice under different refs looks like — a published URL in one document's $schema and a local path on the command line, say.",
  "type": "object",
  "required": ["title"],
  "properties": {
    "title": { "type": "string", "minLength": 1 }
  },
  "additionalProperties": true
}
