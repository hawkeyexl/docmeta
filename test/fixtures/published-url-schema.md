---
$schema: https://hawkeyexl.github.io/docmeta/schemas/okf/0.1.json
type: guide
title: Named by Published URL
---

# Named by Published URL

This file names the built-in OKF schema by the URL the docs site publishes it
at, rather than by its `google:okf:0.1` id. Both must resolve to the same
bundled schema, with no network access.
