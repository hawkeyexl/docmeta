---
title: Legacy page
slug: A1
---

`slug` trips both `minLength` and `pattern` in keywords.schema.json — the pair
that shares one `(schema, instancePath)` and so forced `keyword` into the
fingerprint.
