---
type: reference
---

The passing half of the Action smoke test's pair. `missing-type.md` is the
failing half, and both are checked against `requires-type.schema.json`, so the
smoke job's inputs are ones it owns rather than live docs pages.
