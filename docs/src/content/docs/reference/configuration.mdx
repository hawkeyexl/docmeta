---
title: Configuration reference
description: Every docmeta.config.yaml key (paths, exclude, schemas, overrides, schemaTrust), with types, defaults, discovery rules, and how config merges with CLI flags.
---

import { Aside } from '@astrojs/starlight/components';

A `docmeta.config.yaml` file supplies default targets, excludes, the default
[schema set](/docmeta/reference/schema-resolution/), and optional per-glob overrides, so
CI can run a bare `docmeta validate`. The config is optional; every key has a
default, and docmeta runs without a config when you pass paths and schemas on
the command line.

## Keys

| Key | Type | Default | Description |
|---|---|---|---|
| `paths` | `string[]` | none | Default files, directories, or globs to process when no positional paths are given. |
| `exclude` | `string[]` | none | Globs to exclude from directory and glob walks. Merged with `--exclude` and the [default ignores](/docmeta/reference/cli/#default-ignores). |
| `schemas` | `(string \| {ref, source?, integrity?})[]` | none | Default [schema set](/docmeta/reference/schema-resolution/) applied to every file that no higher-precedence rule matches. See [Schemas](#schemas). |
| `overrides` | `Override[]` | none | Per-glob schema overrides. The first matching entry wins. See [Overrides](#overrides). |
| `baseline` | `string` | none | Path to a [validation baseline](/docmeta/reference/cli/#baselines). Setting it turns on `--baseline` for every run; `--no-baseline` suppresses it for one. A bare `--write-baseline` records into this same file. |
| `allowEmpty` | `boolean` | `false` | Treat an input set that resolves to zero files as success instead of an error (exit 2). Equivalent to `--allow-empty`. See [empty input sets](/docmeta/reference/cli/#empty-input-sets). |
| `respectGitignore` | `boolean` | `true` | Skip files `.gitignore` covers when expanding directories and globs. `--no-gitignore` overrides it for one run. See [`.gitignore`](/docmeta/reference/cli/#gitignore). |
| `offline` | `boolean` | `false` | Never fetch a remote schema. `--offline` turns it on for one run. See [Offline](#offline). |
| `schemaCache` | `SchemaCache` | none | Settings for the [cross-run schema cache](/docmeta/reference/schema-resolution/#the-cross-run-cache). See [Schema cache](#schema-cache). |
| `schemaTrust` | `SchemaTrust` | none | How far a document's own `$schema` is trusted. Absent means `any` — every reference kind, as docmeta has always behaved. See [Schema trust](#schema-trust). |
| `fill` | `Fill` | none | Defaults for [`docmeta fill`](/docmeta/reference/cli/#fill). See [Fill](#fill). |

The top level must be a YAML mapping. `paths` and `exclude` must be lists of
strings; a non-list or a list containing a non-string value is an error
(exit 2). `schemas` takes [two forms](#schemas). `allowEmpty` and
`respectGitignore` must be booleans — a quoted `"false"` is a string, and
accepting it would turn a setting **on** for someone who wrote it off.

<Aside type="caution">
`allowEmpty: true` disables the check that stops a glob which stopped matching
from reporting a permanently green result. Set it only where matching nothing is
genuinely expected — a repo in a shared CI template that may have no docs yet.
</Aside>

### Unknown keys are errors

Any key outside the table above is an operational error (exit 2), naming the
key it did not recognize and listing the ones it accepts. The same rule applies
inside every nested mapping: `schemas[]` entries, `schemaCache`, `schemaTrust`,
and `fill`.

This is what makes a typo visible. A dropped `schemaTust:` leaves a repository
that reads as guarded and is not; a dropped `intergrity:` leaves a schema that
reads as pinned and is not. Neither produces a diagnostic at any verbosity, so
the config file itself is the only evidence — and it looks correct.

<Aside type="caution" title="A key from a newer docmeta will now fail here">
The cost of the rule is the mirror of its benefit. A config written for a newer
docmeta, carrying a key this version does not know, is rejected outright rather
than partly honored. If you share one config across environments, move them to
the same version, or the same version floor, before adding a key that only the
newer one understands.
</Aside>

### Schemas

Each entry in `schemas:` is either a plain reference string or a mapping. Both
forms can appear in the same list.

```yaml title="docmeta.config.yaml"
schemas:
  - google:okf:0.1                                    # string form
  - ref: ./schema/2.1.json                            # mapping form
    source: https://schemas.example.com/house/2.1.json
    integrity: sha256-9f8e7d6c…
```

| Field | Type | Required | Description |
|---|---|---|---|
| `ref` | `string` | yes | What is loaded: a built-in id, a local `.json` path, or a URL. Identical in meaning to the string form. |
| `source` | `string` | no | Where `ref` came from. Recorded by [`schemas vendor`](/docmeta/reference/cli/#schemas-vendor) so a re-vendor knows the URL, and so an error about the file can name it. |
| `integrity` | `string` | no | `sha256-<64 hex characters>` over the file's bytes. A mismatch is an operational error (exit 2). |

The two forms mean exactly the same thing to schema resolution: the `ref` string
is what appears in reports, in [baseline
fingerprints](/docmeta/reference/cli/#baselines), and in `json` output's
`schemas` array. `source` and `integrity` change nothing about *which* schema is
used — only what happens when the file on disk is not the one that was recorded.

Rules, all of them exit 2 when broken:

- An unknown key in the mapping is an error rather than being ignored, because
  a typo such as `intergrity:` would otherwise leave a config that reads as
  pinned and is not.
- `integrity` must match `sha256-<64 hex characters>`. There is one algorithm
  and one encoding; write them with [`schemas
  vendor`](/docmeta/reference/cli/#schemas-vendor) rather than by hand.
- `integrity` is only accepted on a **local file** reference. A built-in has no
  bytes to check, and a URL may be served from the [schema
  cache](#schema-cache), which stores the parsed schema rather than what the
  server sent — so a pin on either could never be verified.

`overrides[].schemas` takes the string form only. Pins live in one place, and an
override that matched a pinned `ref` still gets that pin, because pins are keyed
on the reference.

<Aside type="note">
A local `ref` — and a `source` that is itself a local path — is resolved
relative to **the config file**, exactly like a plain string ref. See [what
relative paths are relative to](#what-relative-paths-are-relative-to).
</Aside>

### `respectGitignore`

Set it to `false` for a repository whose ignored files should still be checked —
a vendored docset, or a generated tree you deliberately validate:

```yaml title="docmeta.config.yaml"
respectGitignore: false
```

Setting it to `true` says the same thing as the default, with one addition: it
records that you *asked*, so a run where git could not answer — no repository,
or no `git` on `PATH` — prints one line to stderr instead of silently checking
everything. Worth doing in a CI image you do not control.

### Offline

`offline: true` stops docmeta fetching a schema over the network at all. A `url`
reference is served from the [schema cache](#schema-cache); one that is not
cached is an operational error (exit 2) naming the URL. Built-in ids and local
`.json` files are unaffected — neither touches the network — so an air-gapped
build against the default schema set works with nothing else configured.

```yaml title="docmeta.config.yaml"
offline: true
```

`--offline` sets it for a single run. There is no flag to turn it back off, so
leave it out of config unless the whole project is meant to run that way.

### Schema cache

`schemaCache` is a mapping of settings for the [cross-run
cache](/docmeta/reference/schema-resolution/#the-cross-run-cache) of schemas
fetched over `http(s)`.

| Key | Type | Default | Description |
|---|---|---|---|
| `ttlHours` | `number` | `24` | Hours a cached schema is served before it is re-fetched. `0` disables the cache entirely, in both directions; the maximum is `8760` (one year). |

```yaml title="docmeta.config.yaml"
schemaCache:
  ttlHours: 1
```

`ttlHours` must be a number between 0 and 8760 (one year); a negative value, a
non-number, YAML's `1e999`, or anything past the upper bound is an error
(exit 2). Both bounds are rejected rather than clamped, and for symmetric
reasons: a negative TTL makes every entry read as stale, while a value large
enough to overflow the internal millisecond arithmetic makes no entry ever read
as stale. Each gives a cache that silently stops doing its job — in opposite
directions — which is the failure this key exists to prevent.

<Aside type="note">
The cache lives in `.docmeta/schema-cache/`, alongside the proposal cache
`fill` keeps in `.docmeta/cache/`. Add `.docmeta/` to your `.gitignore`; neither
directory should be committed.
</Aside>

### Schema trust

`schemaTrust` decides how far a **document** is trusted to choose the schema it
is judged against. A file's own `$schema` sits above config in the [precedence
chain](/docmeta/reference/schema-resolution/#precedence), which is the feature
that makes a document self-describing — and, in a repository that takes outside
pull requests, the thing a contributor can use to opt out of the standard.

| Key | Type | Default | Description |
|---|---|---|---|
| `documentRefs` | `"any" \| "local" \| "none"` | `any` | What a document's `$schema` may name. |
| `hosts` | `string[]` | none | Hosts a document-supplied URL may name. Consulted **only** under `documentRefs: any`; absent means any host. |

```yaml title="docmeta.config.yaml"
schemaTrust:
  documentRefs: local
```

| `documentRefs` | A document's `$schema` may name | A document's `$schema` may not |
|---|---|---|
| `any` (default) | A built-in id, a file inside the repository, or a URL — narrowed by `hosts` when that key is present. | A path outside the repository. |
| `local` | A built-in id, or a file inside the repository. | Any URL. |
| `none` | Nothing — the key is ignored and the config decides. Docmeta prints one line to stderr naming the file and the reference it dropped. | — |

**This key never applies to a reference an operator supplied.** `schemas`,
`overrides[].schemas`, and `-s/--schema` are unfiltered in every mode. Someone
who can edit the config or pass a flag is not the person this key has in mind.

A refused document is **one failing file** (exit 1), not a stopped run: the
refusal is reported as that file's error, so in `github` and `sarif` output the
annotation lands on the offending document in the pull request. Every other file
in the run is validated as usual.

A built-in id keeps working under `local`, which is what makes that mode safe to
turn on: the [self-describing
document](/docmeta/reference/schema-resolution/#the-schema-key-in-a-file)
pattern — `$schema: google:okf:0.1` in the frontmatter — names a schema shipped
inside docmeta, so it reaches nothing.

#### Where "inside the repository" ends

A document-supplied *path* must resolve inside the repository, in every mode
that honors the reference — `any` included, because containment costs no
existing setup anything. `$schema: ../../../../etc/passwd` is refused; a
monorepo package's `$schema: ../shared/house.json` is not, because the boundary
is the **git root** rather than the config's directory.

With no git repository anywhere above the run, the boundary falls back to the
config's own directory and the message says so, so "outside the repository" and
"outside where you happen to be standing" stay distinguishable.

Paths in `schemas` and on `--schema` are never contained. A schema kept beside
the project rather than in it is an ordinary setup, and an operator wrote it.

<Aside type="caution" title="`hosts` is a convenience, not a security boundary">
`fetch` follows redirects, and nothing re-checks the host on the way. An
allowlisted host that answers `302 https://attacker.example/permissive.json`
therefore defeats the list, and closing that would mean re-checking every hop of
every fetch.

Use `hosts` to point at one known publisher and catch mistakes. For a repository
that genuinely distrusts its contributors, the honest setting is
`documentRefs: local`, which has no such hole.
</Aside>

<Aside type="caution" title="Pin the docmeta version in CI">
A version older than 3.11 has never heard of `schemaTrust:`, so it reads the
config, ignores the key, and **silently does nothing** — no guard, no warning.
That is inherent to any new guard: an old release predates it, and cannot
report a key it does not know is missing.

The mitigation is a version floor wherever the gate runs:

```sh
npx -y docmeta@^3.11 validate "docs/**/*.md"
```

A *misspelled* key is no longer part of this: since the [unknown-key
rule](#unknown-keys-are-errors), `schemTrust:` is an error (exit 2) rather than
a no-op, in the version that introduced it and every version after. The
remaining silence is only the older-binary case above, which a version floor is
the answer to.
</Aside>

### Fill

`fill` is a mapping of defaults for the [`fill`](/docmeta/reference/cli/#fill)
command. Every key is optional, and every one is overridden by its CLI flag.

| Key | Type | Default | Description |
|---|---|---|---|
| `provider` | `string` | `auto` | Inference provider: `auto`, `anthropic`, `openai`, `claude-cli`, `llama-cpp`, or `mock`. `auto` [detects one](/docmeta/reference/cli/#fill). An unknown name is an error (exit 2). |
| `model` | `string` | provider default | Model override. Requires `provider` to be set to something other than `auto`, since a model name does not say which provider owns it (exit 2). |
| `confidenceThreshold` | `number` | `0.7` | Minimum self-reported confidence, from 0 to 1, needed to write a value. |
| `maxTurns` | `number` | none | Stop after this many inference calls. Counts **calls, not files**: a long document is split across several. |
| `chunkChars` | `number` | `12000` | Characters of document sent per call. The whole file is always sent; this decides how many calls that takes. |
| `concurrency` | `number` | `4` | Files inferred in parallel. Between 1 and 64. |

A numeric key outside its range, or one that is not a finite number such as
YAML's `1e999`, is an error (exit 2).

```yaml title="docmeta.config.yaml"
fill:
  provider: anthropic
  confidenceThreshold: 0.9
  maxTurns: 200
```

Pinning `provider` is worth doing in CI. Left on `auto`, a runner that loses its
API key silently falls through to a local model and downloads gigabytes rather
than failing the build.

### Overrides

`overrides` is a list of mappings, each applying a schema set to files matching
a glob.

| Field | Type | Required | Description |
|---|---|---|---|
| `files` | `string` | yes | A glob matched against each file's path. |
| `schemas` | `string[]` | yes | The schema set applied to matching files. |

For a given file, docmeta uses the **first** override whose `files` glob matches
(in list order); later matching overrides are ignored. See [schema
resolution](/docmeta/reference/schema-resolution/#precedence) for where overrides sit in
the precedence chain.

## Example

```yaml
# docmeta.config.yaml
paths:
  - "docs/**/*.md"
  - "docs/**/*.mdx"
exclude:
  - "docs/drafts/**"
schemas:
  - google:okf:0.1
overrides:
  - files: "docs/api/**"
    schemas:
      - ./schemas/api.schema.json
  - files: "docs/guides/**"
    schemas:
      - ./schemas/guide.schema.json
```

## Discovery

When you do not pass `-c/--config`, docmeta searches the current working
directory and then each parent directory in turn. In every directory it looks
for these files, in order:

1. `docmeta.config.yaml`
2. `docmeta.config.yml`

The **first file found wins** and the search stops there. Configs are never
merged: `schemas` is a set every file must satisfy in full and `overrides` is
first-match-wins ordered, so a partial merge would quietly redefine what the
contract means. A `docmeta.config.yaml` inside `docs/` therefore *shadows* the
one at the repo root for runs started inside `docs/` — the escape hatch for a
subtree with genuinely different rules. For per-directory rules within one
contract, prefer [`overrides`](#overrides) instead.

The search stops at the **project boundary**: the first directory containing a
`.git` entry, which is itself searched. A git *file* — what a worktree or a
submodule carries — counts as a boundary just as a directory does. If no
ancestor is a repository, only the current working directory is searched;
docmeta never reads a config from your home directory or from `/`.

If no config is found, docmeta runs on the [built-in default schema
set](/docmeta/reference/schema-resolution/#precedence). Pass `--no-config` to
skip discovery deliberately — useful for a one-off run against the defaults.

When you pass `-c/--config <path>` explicitly, no search happens at all and that
file must exist; a missing config file is an error (exit 2).

When a run is governed by a config, docmeta says so before the report:

```
Using docmeta.config.yaml (../..)
```

The path in parentheses is the config's directory relative to where you ran the
command. The line goes to stdout in `pretty` output and to stderr for every
machine format — `json`, `github`, `sarif`, and `junit` — so machine-readable
output stays parseable.

## What relative paths are relative to

A config's relative paths mean what they look like they mean to whoever is
editing the config: they resolve against **the config file's own directory**,
not the directory you happened to run from. That applies to `paths`, `exclude`,
`baseline`, and any local `.json` schema file named in `schemas` or `overrides`.
So a config means the same thing whether you run from the repo root, from
`docs/`, or from a monorepo package script.

This matters most for `baseline`. Resolving it against the working directory
instead would mean a run from a subdirectory quietly found no baseline and
reported the entire recorded backlog as new. For the same reason, the schema
reference inside a [violation fingerprint](/docmeta/reference/cli/#baselines) is
measured against the config's directory too, so a baseline recorded from the
repo root matches one recorded from `docs/`, byte for byte.

Two things stay relative to the **current working directory** instead, because
a person standing in a shell typed them there:

- positional `[paths...]` on the command line, and `-s/--schema`;
- a document's own `$schema` value.

A single run uses *either* positional paths *or* config `paths`, never both, so
there is exactly one base per run. When the run takes its targets from config
`paths`, reported file paths are relative to the config's directory.

## How config merges with CLI flags

CLI flags and config values combine per setting. Some flags override the
corresponding config value; others merge with it.

| Setting | CLI flag | Config key | How they combine |
|---|---|---|---|
| Inputs | `[paths...]` | `paths` | CLI positional paths **override** config `paths` entirely. Config `paths` is used only when no positional paths are given. |
| Excludes | `--exclude` | `exclude` | **Merged.** The effective exclude set is config `exclude` plus every `--exclude` glob plus the [default ignores](/docmeta/reference/cli/#default-ignores). |
| Schemas | `-s/--schema` | `schemas` / `overrides` | CLI `--schema` **overrides** both `schemas` and `overrides` for every file. See [precedence](/docmeta/reference/schema-resolution/#precedence). |
| Config discovery | `--no-config` | — | `--no-config` skips discovery entirely, so no config value applies. It sets the same option as `-c/--config`, so if you pass both, whichever comes later on the command line wins. |
| Empty input sets | `--allow-empty` | `allowEmpty` | The flag **overrides** the config key, and only in the permissive direction: passing it turns the check off. Omitting it falls back to config, then to off. |
| Fill settings | `--confidence`, `--provider`, `--model`, `--max-cost-usd`, `--concurrency` | `fill.*` | Each flag **overrides** its matching `fill` key. An unset flag falls back to config, then to the built-in default. |
| Offline | `--offline` | `offline` | The flag **overrides** the config key, and only in the restrictive direction: passing it turns fetching off. Omitting it falls back to config, then to off. |

<Aside type="caution">
`--schema` is all-or-nothing: passing it once makes that schema set apply to
**every** file in the run, bypassing config `overrides` and per-file `$schema`.
Use config `overrides` when different folders need different schemas.
</Aside>

{/* Doc Detective inline tests: verify this page's documented docmeta behavior
    against the local build, exposed as `docmeta` / `npx docmeta` via `npm link`
    (see .github/workflows/doc-detective.yml), over committed fixtures.
    Run with `npx doc-detective`. Config: .doc-detective.json. */}
{/* test {"testId":"configuration-reference","description":"Config discovery, validation, and merge claims, exercised against the local build."} */}
{/* step {"description":"An explicit -c to a missing file exits 2 rather than falling back to discovery.","runShell":{"command":"docmeta validate -c does-not-exist.yaml test/fixtures/valid.md","exitCodes":[2],"stdio":"Config file not found"}} */}
{/* step {"description":"A config whose paths is not a list of strings is a usage error (exit 2).","runShell":{"command":"docmeta validate -c test/fixtures/dd/bad-config.yaml test/fixtures/valid.md","exitCodes":[2],"stdio":"\"paths\" must be a list of strings"}} */}
{/* step {"description":"--schema overrides config/file schema resolution for every file in the run.","runShell":{"command":"docmeta validate test/fixtures/missing-type.md -s ./test/fixtures/extra.schema.json","exitCodes":[0],"stdio":"1 passed"}} */}
{/* step {"description":"A documentRefs value outside the three modes is a usage error (exit 2), not a silently ignored key.","runShell":{"command":"docmeta validate -c test/fixtures/dd/bad-schema-trust.yaml test/fixtures/valid.md","exitCodes":[2],"stdio":"must be one of"}} */}
{/* step {"description":"documentRefs: local keeps a built-in $schema working — the self-describing document pattern is unaffected.","runShell":{"command":"docmeta validate -c test/fixtures/dd/schema-trust-local.yaml test/fixtures/schema-ref.md","exitCodes":[0],"stdio":"1 passed"}} */}
{/* step {"description":"A misspelled top-level key is a usage error (exit 2), not a silent no-op.","runShell":{"command":"docmeta validate -c test/fixtures/dd/misspelled-top-level-key.yaml test/fixtures/valid.md","exitCodes":[2],"stdio":"unknown key"}} */}
{/* test end */}
