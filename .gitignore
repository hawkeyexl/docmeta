node_modules/
dist/
.husky/_/
*.log
.DS_Store
coverage/
.vitest/

# Doc Detective generated output. The tests live inline in docs/ (.mdx) and the
# config is .doc-detective.json at the repo root; this directory is run output only.
.doc-detective/

# `moose-meta fill` proposal cache — machine-local, safe to delete at any time.
# `.docmeta/` is the pre-rename location; kept ignored for a release or two so
# contributors' stale caches stay untracked.
.moose/
.docmeta/
